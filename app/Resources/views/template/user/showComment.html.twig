{% extends 'base.html.twig' %}
{% block body %}
<div class="row">
    <h3 style="margin: 50px 0; text-align:center">Page des commentaires</h3>
    {% for p in fetchComment %}
        <div class="jumbotron col-md-8 col-md-push-2" style="margin: 50px 0;">
            <h3>{{ p.user  }} | {{ p.date|date('d-m-Y') }}</h3>
            <p>Pour le produit : {{  p.getItem().title  }}</p>
            <h4>Commentaire</h4>
            <p>{{ p.comment }}</p>
        </div>
    {% endfor %}
</div>

    <a href="{{ path('catalogue') }}"><input type="button" class="btn btn-primary" value="retour au catalogue" style="margin: 50px 16%; text-align:center"></a>

<footer class="container-fluid bg-primary" id="footer">
    <div class="row">
        <div class="col-md-4">
            <p>VOIP</p>
            <p>Mainenance Informatique</p>
            <p>Ordinateur</p>
        </div>
        <div class="col-md-4">
            <p>VOIP Shop</p>
            <p>Place du Marché</p>
            <p>1340 Ottignies</p>
            <p>info@voip-shop.be</p>
        </div>
        <div class="col-md-3">
            <p>VOIP Shop</p>
        </div>
    </div>
</footer>
{% endblock %}

